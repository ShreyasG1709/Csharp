using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Enter the first integer:");
        int num1 = int.Parse(Console.ReadLine());

        Console.WriteLine("Enter the second integer:");
        int num2 = int.Parse(Console.ReadLine());

        PerformArithmetic(num1, num2);
    }

    static void PerformArithmetic(int num1, int num2)
    {
        Console.WriteLine("Addition: " + (num1 + num2));
        Console.WriteLine("Subtraction: " + (num1 - num2));
        Console.WriteLine("Multiplication: " + (num1 * num2));
        
        if (num2 != 0) {
            Console.WriteLine("Division: " + ((double)num1 / num2));
        } else {
            Console.WriteLine("Division by zero is undefined.");
        }
    }
}



//////////////////////////////////////
Calculator.PerformArithmetic(num1, num2);
    }
}

public static class Calculator
{
    public static void PerformArithmetic(int num1, int num2)
    {
        Console.WriteLine("Addition: " + (num1 + num2));
        Console.WriteLine("Subtraction: " + (num1 - num2));
        Console.WriteLine("Multiplication: " + (num1 * num2));
        
        if (num2 != 0) {
            Console.WriteLine("Division: " + ((double)num1 / num2));
        } else {
            Console.WriteLine("Division by zero is undefined.");
        }
    }
}
In this program, we have defined a separate static class Calculator that contains a static method PerformArithmetic which takes two integer arguments, num1 and num2. The method performs basic arithmetic operations on these two integers and displays the results using Console.WriteLine().

We call the PerformArithmetic method from the Main method after getting input from the user. Note that we don't need to create an instance of the Calculator class to call the PerformArithmetic method, as it is a static method.

Using a separate class like this can be useful for organizing related functions and methods together.







